# ── Python (client) ───────────────────────────────────────────────────────────

# Bytecode / compiled
__pycache__/
*.py[cod]
*$py.class
*.so

# Build / packaging
build/
dist/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
develop-eggs/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv

# Mask2Former model files (large — auto-downloaded at runtime)
mask2former/models/*.bin
mask2former/models/*.safetensors

# OneFormer model files (large — auto-downloaded at runtime)
oneformer/models/*.bin
oneformer/models/*.safetensors

# Depth Anything model files (large — downloaded at runtime)
depth/models/*.bin
depth/models/*.safetensors

# Cache
.cache/
*.cache

# Testing
.pytest_cache/
.coverage
htmlcov/
