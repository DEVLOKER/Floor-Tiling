# Web framework & server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
aiofiles>=23.2.0

# License verification
cryptography>=42.0.0

# Image processing
# opencv-contrib-* (headless) is required: core/masks.py uses cv2.ximgproc
# (guided filter) for edge-aware mask refinement, which is NOT in plain
# opencv-python. The -headless variant drops GUI deps (server-appropriate).
numpy>=1.26.0
opencv-contrib-python-headless>=4.9.0
Pillow>=10.3.0

# Deep learning (PyTorch CPU-only)
# The extra index ensures pip pulls the lightweight CPU wheels instead of the
# multi-GB CUDA variant from PyPI.  For local GPU dev, override with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0
torchvision==0.21.0

# Transformers — Mask2Former / OneFormer (segmentation), Depth Anything V2
# (depth), Grounding DINO, SAM/SlimSAM, ViTMatte. 4.45+ for the Depth-Anything-V2
# metric models. timm/accelerate are backbone/loader deps several of these need.
transformers>=4.45.0
timm>=1.0.0
accelerate>=0.30.0
safetensors>=0.4.0
scipy>=1.11.0

# Open-vocabulary object detection — YOLO-World (ultralytics). rf-clip + ftfy
# provide the CLIP text encoder that set_classes() needs to bake the prompt into
# the weights (ml/yolo_world.py). ultralytics pulls in opencv-python; the libgl1
# system lib in the Dockerfile satisfies its runtime needs.
ultralytics==8.4.37
rf-clip>=1.1
ftfy>=6.0

# System/hardware utilities
psutil>=7.2.2
pyudev>=0.24.4 # Linux only
# wmi (and pywin32) are Windows-only; for local Windows development, install with:
# pip install wmi==1.5.1 pywin32
