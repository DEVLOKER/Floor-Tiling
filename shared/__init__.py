"""Shared utilities used by the client and admin components.

A regular package (not a namespace package) so its modules compile to native
extensions with correct dotted names (e.g. ``shared.utils.fingerprint``).
"""
